//require is a function that is used to include modules in your application. Modules are reusable pieces of code that you can import into your project to use their functionality. Node.js has a built-in module system that allows you to organize your code and use third-party libraries.
// fs-file system is a module used to reading and writing data. 
const fs = require('fs');  

const hello = 'Hello, wordl!';
    console.log(hello);


